import React from 'react'
import styled from 'styled-components'

export const ChipsList = () => {
  return (<div>Chips List</div>)
}

const StyledComponent = styled(ChipsList)`
`

export default StyledComponent
